# archives générées — reconstruites à la demande depuis les sources
talkandshine-site.zip
talkandshine-netlify.zip

# secrets — ne jamais commiter
.env
.env.*
!.env.example

# dépendances
node_modules/
.netlify/
